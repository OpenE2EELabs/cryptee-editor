# FakeCloudDrive Demo

FakeCloudDrive is a fictional brand used only to demonstrate a cloud-drive style integration. It simulates:

- a file list;
- an encrypted object store;
- signed-URL-style Blob URLs;
- editor launch inside an iframe.

It proves the protocol does not depend on a specific storage product.

